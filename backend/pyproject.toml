[project]
name = "backend"
version = "0.1.0"
description = "PM backend server"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "httpx",
    "python-dotenv",
    "uvicorn[standard]",
]

[dependency-groups]
dev = ["pytest"]
